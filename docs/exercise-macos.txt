------------------------------------------------------------------------
. find files and delete them
  $ find . -type f -name "*.bak" -exec rm -f {} \;

. find what you want
  $ find / -iname LocalSettings.php -print
------------------------------------------------------------------------
. collect and compress
  .. tar 묶기
$ tar cvfpz abc.tar.gz ./abc
 .. tar 풀기
$ tar xvfpz abc.tar.gz
------------------------------------------------------------------------
. view from tar
$ tar tvf abc.tar.gz
------------------------------------------------------------------------
. 5초마다 명령어로 재실행 하기
$ while true; do date; sleep 5; done

------------------------------------------------------------------------
. iterm2 shell integration
You will also have these commands:
imgcat filename
  Displays the image inline.
it2dl filename
  Downloads the specified file, saving it in your Downloads folder.

------------------------------------------------------------------------
. check file type
$ file 1.html
1.html: HTML document text

------------------------------------------------------------------------
. CPU and MEM Info (for Linux)
$ less /proc/cpuinfo
$ less /proc/meminfo

------------------------------------------------------------------------
. flow  --> 김태용 Linux p64
$ cd ~/Downloads
$ mkdir test
$ cd test
$ touch a01.txt a02.txt a03.txt a04.txt a05.txt d01.txt d02.txt
$ cd ..
$ ls -l|grep ^d
$ cd test
$ grep -n ^d *
$ grep -n ^d a?1.???
$ ls [ab]*
$ ls [!ab]*
$ ls *[![:upper:]].*
$ ls [[:lower:]]*
------------------------------------------------------------------------
$ cp -i A01.txt d01.txt

------------------------------------------------------------------------
$ cat /dev/null > c03.txt

------------------------------------------------------------------------
. rename

  .. renames.sh
#!/bin/bash
# renames.sh
# 간단한 파일 이름 바꾸기 프로그램

criteria=$1
re_match=$2
replace=$3

for i in $( ls *$criteria* );
do
src=$i
tgt=$(echo $i | sed -e "s/$re_match/$replace/")
mv $src $tgt
done

  .. rename filename
Batch rename files from lower case file name to upper case filename or vice versa!

Suppose you have a lot of image files in lower case name and you want to change rename them to upper case filename or vice-versa
for file in *.jpg; do mv $file $(echo $file | tr a-z A-Z); done

or from upper case to lower case
for file in *.JPG; do mv $file $(echo $file | tr A-Z a-z); done

If filenames contains spaces in them then just wrap the variable names in "", like
for file in *.jpg; do mv "$file" "$(echo $file | tr a-z A-Z)" ; done

  .. command rename
cd/' *.png   ## 단순히 display 하고자 할 때

------------------------------------------------------------------------
. 표준 입출력 파일 descriptor : 김태용 p81
  .. 표준입력(stdin) - 키보드       0
  .. 표준출력(stdout) - 모니터       1
  .. 표준에러(stderr) - 모니터       2

------------------------------------------------------------------------
. gcloud
gcloud compute --project "hakchin-167514" ssh --zone "us-central1-c" "hakchin@gcp-micro"

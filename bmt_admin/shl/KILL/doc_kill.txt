------------------------------------------------------------------------
Process Kill
------------------------------------------------------------------------
sh kill.sh | psql -e

------------------------------------------------------------------------
끊어지지 않는 Session을 정리하고자 할 때
------------------------------------------------------------------------
세션 정리가 안 되어서 DB STOP이 안 될 때 아래와 같이 했다.

$>all
=> ps -ef | grep postgres | grep con | grep -v grep | awk '{print $2}' | xargs
kill -11

